---
- name: Uninstall Sway
  become: true
  ansible.builtin.package:
    name:
      - sway
    state: absent
  tags:
    - sway

- name: Destroy ~/.config/sway directory
  become: false
  ansible.builtin.file:
    path: ~/.config/sway
    state: absent
  tags:
    - sway
