---
zsh:
  terminal: alacritty
user: "{{ lookup('env', 'USER') }}"
theme: robbyrussell
editor: vim
browser: chromium
lang: en_US.UTF-8
path:
  - ~/bin
plugins:
  - sudo
  - git
aws:
  access_key_id:
  secret_access_key:
chef:
  user:
  url:
  repo:
  log_level:
python_force_color: 1
ansible_force_color: 1
