---
- hosts: all
  become: true
  become_method: sudo
  vars_files:
    - "./vars/{{ inventory_hostname }}.yml"
  roles:
    - setup
    - mkinitcpio
    - hostname
    - locale
    - timezone
    - yaourt
    - pkgs
    - vim
    # - i3
    # - slim
  tasks:

#  # Services
#  - name: Enable Slim
#    service: name=slim
#             enabled=yes
#             state=started
#    tags:
#      - service
#
#  - name: Enable ntpdate
#    service: name=ntpdate
#             enabled=yes
#             state=started
#    tags:
#      - service
